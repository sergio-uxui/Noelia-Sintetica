# Noelia Sintética — Dependencias Python
# Versiones testeadas con Streamlit Community Cloud

# API de Claude (Anthropic)
anthropic>=0.88.0

# HTTP y scraping (para páginas de empresa)
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Búsqueda de empleo via SerpApi (Google Jobs — agrega LinkedIn, Indeed, InfoJobs…)
serpapi>=1.0.2

# Planificador
schedule>=1.2.0

# Configuración
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Terminal UI (CLI)
rich>=13.7.0

# Dashboard web
streamlit>=1.35.0
plotly>=5.22.0

# Utilidades de fechas
python-dateutil>=2.9.0
